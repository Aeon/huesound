To install the huesound database, you'll need Postgres 8.3 or greater
with the cube extension installed. 

pg_config will need to be on your path.

Run setup.sh to create the database and prepare it for use

